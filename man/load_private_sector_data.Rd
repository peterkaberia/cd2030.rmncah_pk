% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_survey_data.R
\name{load_private_sector_data}
\alias{load_private_sector_data}
\title{Load and Prepare Private Sector Data}
\usage{
load_private_sector_data(
  path = NULL,
  .data = NULL,
  country_iso,
  level = c("national", "area")
)
}
\arguments{
\item{path}{Optional. File path to a \code{.dta} file. Required if \code{.data} is not provided.}

\item{.data}{Optional. A pre-loaded dataset. Used directly if provided.}

\item{country_iso}{Required. ISO3 country code to filter the data.}

\item{level}{Either \code{"national"} or \code{"area"} indicating the data level.}
}
\value{
A tibble of class \code{"cd_private_sector_data"} with a \code{"level"} attribute.
}
\description{
Loads raw prevalence data for public/private sector analysis and applies
internal preprocessing for c-section share calculation.
}
\details{
This function:
\itemize{
\item Loads and filters data for the specified \code{country_iso}.
\item Assigns \code{"Public"} or \code{"Private"} to the \code{sector} column based on \code{indic} suffix.
\item Removes sector suffix from \code{indic}.
\item Computes number of c-sections (\code{num_csection}) and sector share of c-sections (\code{share_csection})
within groupings defined by \code{iso} or \code{iso + area}.
\item If level is \code{"area"}, parses \code{level} string into \code{area = "Urban"} or \code{"Rural"}.
}
}
\section{Output columns}{

\itemize{
\item \code{indic}: cleaned indicator name (without "pub"/"priv")
\item \code{sector}: \code{"Public"} or \code{"Private"}
\item \code{share_csection}: share of c-sections attributable to each sector
\item \code{area}: (only if \code{level == "area"})
}
}

\examples{
\dontrun{
dt <- load_private_sector_data(
  path = "National estimates.dta",
  country_iso = "KEN",
  level = "national"
)
}

}
\seealso{
\code{\link[=load_csection_estimates]{load_csection_estimates()}}, \code{\link[=prepare_private_sector_plot_data]{prepare_private_sector_plot_data()}}
}
