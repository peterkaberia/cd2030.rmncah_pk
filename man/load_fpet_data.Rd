% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_survey_data.R
\name{load_fpet_data}
\alias{load_fpet_data}
\title{Load FPET Data from CSV File or Use Provided Data}
\usage{
load_fpet_data(path = NULL, .data = NULL, country_iso = NULL)
}
\arguments{
\item{path}{Optional \code{character}. File path to a \code{.csv} file. If provided,
data is read from the file.}

\item{.data}{Optional \code{data.frame} or \code{tibble}. Used directly if provided
instead of loading from file.}

\item{country_iso}{Optional \code{character}. ISO3 country code. Used to tag or
filter the dataset.}
}
\value{
A tibble with class \code{cd_fpet_data}. If \code{country_iso} is provided, the
data is filtered by \code{iso3 == country_iso}.
}
\description{
\code{load_fpet_data} loads Family Planning Estimation Tool (FPET) data from a \code{.csv}
file, or uses a pre-supplied dataset. It assigns a country ISO code if specified,
and returns a tibble with a custom class \code{cd_fpet_data}.
}
\examples{
\dontrun{
# Load FPET data from file
fpet <- load_fpet_data(path = "path/to/fpet.csv", country_iso = "NGA")

# Use existing data
fpet <- load_fpet_data(.data = some_fpet_data, country_iso = "ETH")
}

}
